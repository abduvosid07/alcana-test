/** @type {import('next').NextConfig} */
const nextConfig = {
  typescript: {
    // This tells Vercel to ship your successfully compiled code immediately!
    ignoreBuildErrors: true,
  },
};

export default nextConfig;
